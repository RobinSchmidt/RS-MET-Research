/* 
find coefficients for the function 
y = f(x) = c2 * x^2 + c1*x + c0 
such that 
f(x1) = y1, f(x2) = y2, f(x3) = y3:
*/

e1: y1 = c2 * x1^2 + c1 * x1 + c0;
e2: y2 = c2 * x2^2 + c1 * x2 + c0;
e3: y3 = c2 * x3^2 + c1 * x3 + c0;
solve([e1, e2, e3], [c0, c1, c2]);

