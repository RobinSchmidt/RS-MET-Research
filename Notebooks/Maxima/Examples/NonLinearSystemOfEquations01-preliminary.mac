/* 
find coefficients for the function 
y = f(x) = c1 * exp(c2 * x + c3) + c4  
such that 
f(x1) = y1, f(x2) = y2:
*/

e1: y1 = c1 * exp(c2 * x1 + c3) + c4;
e2: y2 = c1 * exp(c2 * x2 + c3) + c4;
solve([e1, e2], [c1, c2, c3, c4]);

